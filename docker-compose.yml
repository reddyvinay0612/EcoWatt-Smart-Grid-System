version: '3.8'

services:
  db:
    image: postgres:15-alpine
    container_name: ecowatt_db
    restart: always
    environment:
      POSTGRES_USER: ecowatt_user
      POSTGRES_PASSWORD: ecowatt_secure_password
      POSTGRES_DB: ecowatt_production
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ecowatt_user -d ecowatt_production"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: ecowatt_backend
    restart: always
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=postgresql://ecowatt_user:ecowatt_secure_password@db:5432/ecowatt_production
      - SIMULATION_INTERVAL_SECONDS=15
      - SECRET_KEY=supersecretkeyforecowattaidonotuseinproduction12345!
    volumes:
      - ./backend:/app
    depends_on:
      db:
        condition: service_healthy

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: ecowatt_frontend
    restart: always
    ports:
      - "80:80"
    depends_on:
      - backend

volumes:
  postgres_data:
